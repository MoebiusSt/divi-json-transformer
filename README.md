# divi-json-transformer
Transform DIVI JSON exports. Split long DIVI text modules which contain HTML text with headings or lists into several text modules or rows at definable split points. A free, open-source web application tool that works entirely in your browser. No dependencies.
